<tr id="<%= dom_id room %>">
  <td>
    <%= link_to truncate(room.name), room, class: 'text-dark fw-bold' %>
  </td>
  <td>
    <%= room.created %>
  </td>
  <td>
    <%= link_to truncate(room.user.name), room.user, class: 'text-info fw-bold' %>
  </td>
  <!-- <td class="text-end">
    <%# if can? :manage, room %>
      <%#= link_to room_path(room), method: :delete, data: { confirm: 'Are you sure you want to delete this room?' } do %>
        <i class='fas fa-trash text-dark me-1'></i>
      <%# end %>
    <%# end %>
  </td>   -->
</tr>
