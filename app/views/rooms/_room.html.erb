<tr id="<%= dom_id room %>">
  <td><%= link_to truncate(room.name), room, class: 'text-dark font-weight-bold' %></td>
  <td>
    <%= ActionController::Base.helpers.distance_of_time_in_words Time.now.to_date, room.created_at %> ago
    <%#= room.created_at.in_time_zone("#{current_user.timezone}").strftime('%b %-d, %Y') %>
  </td>
  <td>
    <%= truncate(room.user.name) %>
  </td>
  <!-- <td class="text-right">
    <%# if can? :manage, room %>
      <%#= link_to room_path(room), method: :delete, data: { confirm: 'Are you sure you want to delete this room?' } do %>
        <i class='fas fa-trash text-dark mr-1'></i>
      <%# end %>
    <%# end %>
  </td>   -->
</tr>
