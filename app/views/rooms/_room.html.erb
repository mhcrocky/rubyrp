<tr id="<%= dom_id room %>">
  <td>
    <%= link_to truncate(room.name), room, class: 'text-dark font-weight-bold' %>
  </td>
  <td>
    <%= room.created %>
  </td>
  <td>
    <%= link_to truncate(room.user.name), room.user, class: 'text-info font-weight-bold' %>
  </td>
  <!-- <td class="text-right">
    <%# if can? :manage, room %>
      <%#= link_to room_path(room), method: :delete, data: { confirm: 'Are you sure you want to delete this room?' } do %>
        <i class='fas fa-trash text-dark mr-1'></i>
      <%# end %>
    <%# end %>
  </td>   -->
</tr>
