<%= render partial: 'articles/form' %>

<section class="jumbotron py-4 text-center">
  <p>
    <%= link_to 'Remove', @article, method: :delete, data: { confirm: 'Are you sure you want to delete this article?' }, class: 'btn btn-outline-danger btn-block' %>
  </p>
</section><!-- .jumbotron .text-center -->
